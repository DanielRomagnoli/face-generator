# Face Generator

A DCGAN face generation application built from scratch using PyTorch, FastAPI, and React.

## Project Structure

- `training/` — DCGAN training code
- `backend/` — FastAPI inference API
- `frontend/` — React web application
- `models/` — trained generator models